export async function generateScheduleRecommendation(
  schedule: string,
  preferences: Record<string, string>,
) {
  // change this to return the new schedule. **Make sure that it is reutrning a string that can be parsed into a JSON, just like the in schedule.**
  return schedule;
}
